# Anomalica content

Parent Product and root Core instructions are loaded through `opencode.json` and remain
mandatory. This repository holds assembled articles and their human-owned review or
directive inputs. Read `README.md` and the public content-lifecycle contract before edits.
Fix generated output at its producer in `../assembler/` and regenerate it; preserve
human-owned frontmatter and review sidecars. Do not manually repair derived content in
place of a producer fix.
